{% extends 'base.html.twig' %}

{% block title %}Videolist{% endblock %}

{% block body %}
    <h1>Videolist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ videolist.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ videolist.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ videolist.description }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ videolist.created }}</td>
            </tr>
            <tr>
                <th>Cover</th>
                <td>{{ videolist.cover }}</td>
            </tr>
            <tr>
                <th>Added</th>
                <td>{{ videolist.added }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('videolist_index') }}">back to list</a>

    <a href="{{ path('videolist_edit', {'id': videolist.id}) }}">edit</a>

    {{ include('videolist/_delete_form.html.twig') }}
{% endblock %}
