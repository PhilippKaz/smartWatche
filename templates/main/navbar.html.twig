{# "Шапка сайта" #}
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation" style="background-color: #31353D">
        <div class="container-fluid">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>

                </div>
                <div class="collapse navbar-collapse" id="myNavbar">
                    <div>
                        <a class="navbar-brand" href="/">ｓｍａｒｔＷａｔｃｈｅｒ</a>
                        <ul class="nav navbar-nav" style="margin-left: 48px;">
                            <li><a href="/category/">Категории</a></li>
                            <li><a href="/projects">Проекты</a></li>
                            <li><a href="/videos" >Видео</a></li>
                            {% if (is_granted("ROLE_USER")) and (is_granted("ROLE_ADMIN")) %}
                                <li><a href="/adminPanel">Панель администратора</a></li>
                                <li>
                                    <div class="">
                                        {% if app.session.get('theme') %}{{ app.session.get('theme')['title']}}>{% else %}{% endif %}
                                        <form method="get" action="/change_theme">
                                            <button type="submit" id = "toggle-theme"><i style="margin-top: 18px; width: 50px" class = "glyphicon glyphicon-adjust"></i></button>
                                        </form>
                                    </div>

                                  {#  <a type="button" id="toggle-theme"></a>#}
                                </li>
                            {% elseif (is_granted("ROLE_USER")) and (is_granted("ROLE_ADMIN") == false)%}
                                <li><a href="/userPanel">Панель пользователя</a></li>
                                <li>
                                    <div class="">
                                        {% if app.session.get('theme') %}{{ app.session.get('theme')['title']}}>{% else %}{% endif %}
                                        <form method="get" action="/change_theme">
                                            <button type="submit" id = "toggle-theme"><i style="margin-top: 2.4vh; width: 50px" class = "glyphicon glyphicon-adjust"></i></button>
                                        </form>
                                    </div>

                                    {#  <a type="button" id="toggle-theme"></a>#}
                                </li>
                            {% endif %}
                        </ul>
                    </div>

                    <ul class="nav navbar-nav navbar-right">
                        {% if user != null %}
                            {% if is_granted("ROLE_USER") %}
                                <li><a> Вы зашли как, {{ user }} </a></li>
                                <li>
                                    <a  onclick="$.removeCookie('theme');" href="{{ path('fos_user_security_logout') }}"> <i class="glyphicon glyphicon-log-out"></i>{{ ' Выйти'|trans({}, 'FOSUserBundle') }}</a>
                                </li>
                            {% else %}
                                <li><a href="{{ path('fos_user_security_login') }}"><i class="glyphicon glyphicon-log-in"></i>{{ ' Войти'|trans({}, 'FOSUserBundle') }}</a></li>
                            {% endif %}
                        {% else %}
                            <li><a href="{{ path('fos_user_security_login') }}"><i class="glyphicon glyphicon-log-in"></i>{{ 'Войти'|trans({}, 'FOSUserBundle') }}</a></li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </div>
</nav>


